
$(document).ready(() => {
    const slickOptions = {
      infinite: true,
      autoplay: true, 
      autoplaySpeed: 3000,
      dots: false,   
    };
    $(".slider").slick(slickOptions){
    
  });
